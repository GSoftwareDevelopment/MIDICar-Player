gotoNEntry:
    txa:pha

    lda #<LIST_ADDR
    sta fileList
    lda #>LIST_ADDR
    sta fileList+1

    ldx nEntry
    beq exitGotoNEntry

loop:
    jsr nextEntry
    beq exitGotoNEntry
    dex
    bne loop

exitGotoNEntry:

    pla:tax
