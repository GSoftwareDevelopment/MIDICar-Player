colptr = $88

  lda #<colors_addr
  sta colptr
  lda #>colors_addr
  sta colptr+1

  lda screenStatus
  and #ss_colorSet
  ora colptr
  sta colptr

skip:
  ldy #7
copyColors:
  lda (colptr),y
  sta $D013,y
  sta 705,y
  dey
  bpl copyColors
